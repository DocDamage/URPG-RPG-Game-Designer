# URPG Sample Echo Mod

This fixture is a minimal SDK sample used by automated validation. It registers one local command,
reads project data through approved APIs, and emits expected diagnostics without requesting network,
native interop, or arbitrary filesystem write permissions.
