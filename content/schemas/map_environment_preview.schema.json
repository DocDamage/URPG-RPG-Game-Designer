{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://urpg.dev/schemas/map_environment_preview.schema.json",
  "title": "URPG map environment preview",
  "type": "object",
  "required": ["schema", "map_id", "width", "height", "base_weather", "tile_layers", "regions", "tactical_overlay", "spawn_table"],
  "properties": {
    "schema": { "const": "urpg.map_environment_preview.v1" },
    "map_id": { "type": "string", "minLength": 1 },
    "width": { "type": "integer", "minimum": 1 },
    "height": { "type": "integer", "minimum": 1 },
    "base_weather": { "type": "string", "enum": ["clear", "rain", "snow", "storm", "fog"] },
    "base_light": { "$ref": "#/$defs/light" },
    "base_fog": { "$ref": "#/$defs/fog" },
    "base_post_fx": { "$ref": "#/$defs/post_fx" },
    "tile_layers": {
      "type": "array",
      "items": { "$ref": "#/$defs/tile_layer" }
    },
    "regions": {
      "type": "array",
      "items": { "$ref": "#/$defs/region" }
    },
    "parallax_layers": {
      "type": "array",
      "items": { "$ref": "#/$defs/parallax_layer" }
    },
    "smoke_emitters": {
      "type": "array",
      "items": { "$ref": "#/$defs/smoke_emitter" }
    },
    "terrain_mesh_rules": {
      "type": "array",
      "items": { "$ref": "#/$defs/terrain_mesh_rule" }
    },
    "edge_scroll_camera": { "$ref": "#/$defs/edge_scroll_camera" },
    "tactical_overlay": { "$ref": "#/$defs/tactical_overlay" },
    "spawn_table": { "$ref": "#/$defs/spawn_table" }
  },
  "$defs": {
    "vec3": {
      "type": "array",
      "minItems": 3,
      "maxItems": 3,
      "items": { "type": "number" }
    },
    "light": {
      "type": "object",
      "required": ["id", "intensity", "range", "position", "color"],
      "properties": {
        "id": { "type": "integer", "minimum": 0 },
        "intensity": { "type": "number", "minimum": 0 },
        "range": { "type": "number", "exclusiveMinimum": 0 },
        "position": { "$ref": "#/$defs/vec3" },
        "color": { "$ref": "#/$defs/vec3" }
      }
    },
    "fog": {
      "type": "object",
      "required": ["density", "start_distance", "end_distance", "color"],
      "properties": {
        "density": { "type": "number", "minimum": 0 },
        "start_distance": { "type": "number" },
        "end_distance": { "type": "number" },
        "color": { "$ref": "#/$defs/vec3" }
      }
    },
    "post_fx": {
      "type": "object",
      "required": ["exposure", "bloom_threshold", "bloom_intensity", "saturation"],
      "properties": {
        "exposure": { "type": "number" },
        "bloom_threshold": { "type": "number" },
        "bloom_intensity": { "type": "number" },
        "saturation": { "type": "number" }
      }
    },
    "region": {
      "type": "object",
      "required": ["id", "x", "y", "width", "height", "weather", "lighting", "light", "fog", "post_fx"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "x": { "type": "integer", "minimum": 0 },
        "y": { "type": "integer", "minimum": 0 },
        "width": { "type": "integer", "minimum": 1 },
        "height": { "type": "integer", "minimum": 1 },
        "weather": { "type": "string", "enum": ["clear", "rain", "snow", "storm", "fog"] },
        "lighting": { "type": "string", "enum": ["day", "night"] },
        "light": { "$ref": "#/$defs/light" },
        "fog": { "$ref": "#/$defs/fog" },
        "post_fx": { "$ref": "#/$defs/post_fx" }
      }
    },
    "tile_layer": {
      "type": "object",
      "required": ["id", "visible", "locked", "collision", "navigation", "draw_order", "tiles"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "visible": { "type": "boolean" },
        "locked": { "type": "boolean" },
        "collision": { "type": "boolean" },
        "navigation": { "type": "boolean" },
        "draw_order": { "type": "integer" },
        "tiles": { "type": "array", "items": { "type": "integer" } }
      },
      "additionalProperties": false
    },
    "parallax_layer": {
      "type": "object",
      "required": ["id", "asset", "lock_mode", "blend_mode", "opacity", "scroll_x", "scroll_y", "region_id", "draw_order"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "asset": { "type": "string", "minLength": 1 },
        "lock_mode": { "type": "string", "enum": ["map", "screen"] },
        "blend_mode": { "type": "string", "enum": ["normal", "add", "multiply", "screen"] },
        "opacity": { "type": "number", "minimum": 0, "maximum": 1 },
        "scroll_x": { "type": "number" },
        "scroll_y": { "type": "number" },
        "region_id": { "type": "integer", "minimum": 0 },
        "draw_order": { "type": "integer" }
      },
      "additionalProperties": false
    },
    "smoke_emitter": {
      "type": "object",
      "required": ["id", "region_id", "intensity", "size", "opacity", "color", "bloom", "drift_x", "drift_y"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "region_id": { "type": "integer", "minimum": 0 },
        "intensity": { "type": "integer", "minimum": 0, "maximum": 100 },
        "size": { "type": "number", "exclusiveMinimum": 0 },
        "opacity": { "type": "integer", "minimum": 0, "maximum": 255 },
        "color": { "type": "string", "pattern": "^#[0-9A-Fa-f]{6}$" },
        "bloom": { "type": "boolean" },
        "drift_x": { "type": "number" },
        "drift_y": { "type": "number" }
      },
      "additionalProperties": false
    },
    "terrain_mesh_rule": {
      "type": "object",
      "required": ["id", "region_id", "height", "curve", "walkable"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "region_id": { "type": "integer", "minimum": 0 },
        "height": { "type": "number", "minimum": 0 },
        "curve": { "type": "number" },
        "walkable": { "type": "boolean" }
      },
      "additionalProperties": false
    },
    "edge_scroll_camera": {
      "type": "object",
      "required": ["enabled", "keyboard_pan", "click_follow", "speed_tiles_per_second", "margin_pixels", "recenter_hotkey"],
      "properties": {
        "enabled": { "type": "boolean" },
        "keyboard_pan": { "type": "boolean" },
        "click_follow": { "type": "boolean" },
        "speed_tiles_per_second": { "type": "number", "exclusiveMinimum": 0 },
        "margin_pixels": { "type": "integer", "minimum": 0 },
        "recenter_hotkey": { "type": "string" }
      },
      "additionalProperties": false
    },
    "tactical_overlay": {
      "type": "object",
      "required": ["enabled", "origin_x", "origin_y", "move_range"],
      "properties": {
        "enabled": { "type": "boolean" },
        "origin_x": { "type": "integer" },
        "origin_y": { "type": "integer" },
        "move_range": { "type": "integer", "minimum": 0 }
      },
      "additionalProperties": false
    },
    "spawn_table": {
      "type": "object",
      "required": ["id", "entries"],
      "properties": {
        "id": { "type": "string" },
        "entries": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["id", "enemy_id", "x", "y", "cooldown_seconds", "persistent"],
            "properties": {
              "id": { "type": "string", "minLength": 1 },
              "enemy_id": { "type": "string", "minLength": 1 },
              "x": { "type": "integer" },
              "y": { "type": "integer" },
              "cooldown_seconds": { "type": "integer", "minimum": 0 },
              "persistent": { "type": "boolean" }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    }
  }
}
