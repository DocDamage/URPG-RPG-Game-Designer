# ADR-010: Memory and Allocation Model (Unratified Placeholder)

**Status:** Unratified placeholder retained only for ADR numbering continuity and traceability. This file does not record an approved decision and must not be read as an active ADR or current policy source.

**Reader guidance:** treat this page as an archived stub that preserves the fact that ADR-010 was reserved but never ratified in this file. For current execution truth, defer to the canonical program/remediation docs and the live implementation state.

## Context
Frame-by-frame allocation must not cause performance regressions or spikes.

## Historical placeholder
ADR-010 was drafted as a placeholder for a future memory/allocation decision, but no ratified decision text was ever recorded here.

The draft direction recorded here was:
- use per-frame arenas for `PresentationFrameIntent`
- keep stable authoring data on longer-lived storage rather than per-frame allocations

## Rationale
Zero-alloc steady state for the hot path.

## Consequences
- No `new` / `malloc` in the frame generation loop.
- Arena size must be bounded per tier.
