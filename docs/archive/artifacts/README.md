# Archived Artifacts

This folder keeps small tracked output artifacts that were previously loose in the repo root but are no longer active working files.

These files are reference-only and should not be treated as current validation evidence. Fresh validation artifacts should be regenerated from the active build and test workflows.
