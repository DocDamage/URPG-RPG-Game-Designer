# Known Debt Map

This file is a pointer map, not the debt database.

## Canonical Sources

- Program status snapshot: `docs/PROGRAM_COMPLETION_STATUS.md`
- Known debt index: `docs/PROGRAM_COMPLETION_STATUS.md`
- Release execution plan: `docs/release/AAA_RELEASE_EXECUTION_PLAN.md`
- Release readiness matrix: `docs/release/RELEASE_READINESS_MATRIX.md`
- App release readiness matrix: `docs/APP_RELEASE_READINESS_MATRIX.md`

## Current Agent Priorities

- Prefer the active creator-product plan over broad subsystem expansion: `docs/superpowers/plans/2026-07-14-product-feature-usability-native-absorption-plan.md`.
- PFU-00A's repository identity guard is implemented at `tools/ci/check_workspace_identity.ps1`. Run it with `-ExpectedRoot 'G:\URPG Maker-development'` before creator-product changes; its default clean-worktree enforcement is intentional. PFU-00B remains open because its referenced canonical source roadmap is absent from `docs/external-intake/` and must be restored or explicitly re-sourced before a ledger can be truthfully frozen.
- Treat M0-M5 as a bounded implemented foundation, not a completed product. Perspective 2D and the Character Creator draft now register with the shared dirty-state owner. Attached image assets can be dropped into Character Creator’s explicit appearance slots with owner-local undo/redo; remaining M1/M3/M4 breadth includes other durable non-Map authoring surfaces, additional contextual asset owners, completing shortcut coverage, and recording graphical route/layout equivalence.
- M6 now has an initial native session foundation: an editor-owned runtime child, private current-map overlay, authored player-start or selected-part target (with blank-starter fallback), explicit map/spawn/session manifest, visible target/elapsed/exit/overlay state, F5 stop/restart, document/catalog launch blocking, JSONL diagnostics ingestion with matching GridParts-object focus, and return without discarding editor state. It remains incomplete: broader runtime blocker policy, map-entrance/checkpoint targets, generic diagnostic focus, and safe hot reload are still open.
- M7 now has checksummed, ignored snapshot primitives, orderly-session markers, five-minute bounded private capture of dirty Grid Parts, Perspective 2D, ability, Character Creator, quest, database, vendor, audio-mix, and input-remap drafts, plus in-editor count, safe restore-to-new-folder, and clean-session open-recovered-project actions. It remains incomplete: broader durable-surface coverage, explicit governed-asset relinking, and a creator walkthrough.
- M8 has initial Character Creator, ability, event, quest, database, vendor, battle-preview, audio-mix, input-remap, and accessibility-audit slices: Map opens the active project's durable character draft or Ability Inspector without losing Map context; its Perspective 2D event form authors an event, first page, and a supported native command (text/choice dialogue, transfer, state, inventory, common event, encounter launch, or vendor open) in the normal Map document/save/recovery path; its event runtime feeds switch, variable, inventory, and dialogue-choice state into the contextual quest preview, while an encounter launch opens the native Battle Inspector with the active character and ability draft queued and a vendor-open command resolves the native vendor stock preview by stable ID; a marked Battle Inspector preview victory feeds the quest preview only and is explicitly not a playtest combat claim; its contextual encounter control provides the same direct preview route; its contextual audio control applies and saves a project mix preset through the native audio core and assigns an attached audio asset with owner-local undo/redo. The native audio backend now resolves that stable ID through a validated project attachment manifest and project-contained audio payload after preserving explicit direct-path compatibility but before its legacy filename probes, so a governed attached audio asset can reach the same preview/runtime core without treating raw paths as authoritative. Its contextual input control edits, saves, recovers, and restores defaults through the versioned native input-remap store; its contextual accessibility control audits the active audio-mix and battle-preview surfaces through the native auditor; its quest form authors, previews, saves, and recovers a typed objective graph; and its database/vendor form authors a project item and database-validated vendor stock. Each authored event exposes its existing native preview/runtime execution controls. Battle-result, broader audio cue routing, device/manual playback qualification, and the WYSIWYG done rule remain open. The Capybara repository is `reference_only`; any adopted concept needs URPG-native ownership, provenance/license evidence, deterministic tests, and no parallel browser/npm/cloud/save/pathfinding stack.
- M9 has a governed `Lantern of the Willow` contract, a native-wizard draft seed, a guarded native Map switcher for both seeded maps, and a strict report gate, but no creator-authored passed report. M9-M10 must still author/round-trip the listed contextual routes, prove the slice, and then complete manual graphical, accessibility, input, and reference-machine performance review. Headless/model coverage is not manual signoff.
- M11 requires fresh clean-clone/local gates, exact project-selected asset hydration, external platform/toolchain evidence, and a new release-owner decision. The current branch is post-`v0.1.0` and is not a broader all-features or follow-up public release claim.
- PFU-I1 has a separate target qualification contract, strict report checker, and `tools/ci/check_pfu_i1_qualification.ps1` wrapper. Its native target-report emitter now exercises project creation/session, governed attachment, map history/dirty save, playtest return, snapshot, and package-smoke evidence after the wrapper supplies clean commit/build provenance. PFU-I1 remains unsatisfied until the full wrapper produces clean, commit-matched Debug/Release evidence; the historical creator-journey report remains mixed passed/partial/deferred.
- PCQ-701 primary-route work now has governed provisional budgets and raw-trace validation; command search, constrained asset filtering, staged promotion-catalog materialization, and project-reference queries use revisioned indexes. Command search and an explicitly initiated asset catalog job advance at most 64 candidates/items per visible editor frame, and catalog publication is atomic with stale-revision rejection. Default asset report/shard discovery and JSON parsing, project-graph discovery/parsing/mutation rebuilds, project open, map edit, save, playtest launch, package routing, repeated target-hardware traces, approved budgets, and signoff remain open; deterministic fixtures are not latency baselines.
- Product-completion Wave A now has machine-readable gap, document-owner, reference, migration, widget-state, and scale matrices. The project-operation coordinator journals prepared/committed/aborted owner snapshots and rolls back a failed durable acknowledgement; Asset and Economy provide real typed adapters, but the remaining golden-slice owners and startup restoration are open. All 14 registered durable editor documents expose explicit compare/reload/keep-local handling, while stable-ID-changing reloads fail closed where the document embeds its identity; reference-aware rename review remains open. Ctrl+P combines frame-sliced command search with indexed project-object results after a background graph refresh, and startup Settings applies persisted 50%-300% font/style, high-contrast, and reduced-motion changes live. Full document-level incremental graph updates, shared-widget adoption, fixed-geometry conversion, route snapshots, broader error-card conversion, migration fixtures, and manual qualification remain open.
- Product-completion Wave B has a validated 16-row gap matrix with five requirements proved and 11 partial. The live asset catalog query is frame-sliced and cancellable, all required use-here routes commit through typed project operations, notices publish atomically and fail closed on unresolved rights, world authoring persists with reviewed impacts/history, prefab and prop/event batch edits route through their live Map owners, and runtime teleport commands are acknowledged through the editor/runtime bridge. Real export consumption, runtime/package round trips for remaining asset/spatial owners, target-hardware budgets, and manual candidate evidence remain open; the existing 308,833-file export corpus also makes the full export preset exceed the current local verification window and must be addressed in Wave E without hiding the scale cost.
- Product-completion Wave C has a validated 21-row gap matrix with all 21 implementation requirements proved. Governed event, bad-event, branching-quest, database, menu, and cross-editor semantic matrices are executable evidence. Branching quests round-trip through project and installed-package owners; database item/actor batches commit through the typed database authority; economy review reports publish atomically; and the visible menu authoring workspace owns persistence and derives the native runtime projection, with all 11 original starters passing five responsive target presets. Event templates persist as one atomic revisioned project aggregate, commit reviewed update/delete/replacement through shared project history, and expose picker and impact state through the production event-authoring model. Live event traces cross a bounded revisioned runtime/editor process bridge into Diagnostics with pause state, filters, parallel lanes, exact source opening, and guarded controls. Database rename, safe-delete, and replacement now require the authoritative record owner plus every affected typed document owner, expose visible impact state, and prove all 13 kinds plus cross-document reopen and shared undo/redo. Manual visual/accessibility evidence, clean-candidate provenance, and release signoff remain open.
- Product-completion Wave D has a validated 22-row gap matrix with seven requirements proved, 11 partial, and four external. Current-map hot reload crosses the live revisioned editor/runtime bridge and resets affected MapScene state only after safe overlay admission; support bundles consume live session evidence and require a visible redaction preview plus explicit local-write approval; and the accessibility auditor and known-bad corpus remain proved. The live playtest event-debugger lane transports bounded execution and conditional-breakpoint controls plus exact source, stack, watch, breakpoint, and trace snapshots into a visible Diagnostics route that focuses the authoritative editor command. The live runtime-state lane transports revisioned snapshots and guarded acknowledgements, preserves immutable packaged state beneath disposable edits, and exposes temporary watches, mutations, and checkpoint reset. The production scenario lane persists semantic InputCore capture with exact project/runtime identity, replays it headlessly or interactively from the launch MapScene checkpoint, and reports exact divergence. EngineShell now supplies real input/update/render/frame timings, the Windows runtime supplies private-process memory, and the visible live capture lane persists and attributes hitches by subsystem and timestamp. Remaining recovery-adapter breadth, concrete presentation consumers and captures, physical-device/manual accessibility and localization qualification, final licensed assets/fonts/audio, complex-script shaping, release-owner approvals, clean-candidate provenance, and release signoff remain open.
- Product-completion Wave E has a validated 15-row gap matrix with 14 partial and one external requirement. The governed qualification matrix stays synchronized with every native stress, fault, security, sanitizer/fuzz, platform, compatibility, and package authority. Export packaging no longer enumerates the 308k global promoted library by default: only explicit safe bundle IDs are resolved and recorded, and the full export preset now completes. Real repeated target-hardware baselines, process-level stress/fault adapters, sanitizer/fuzzer artifacts, packet capture, transitive SBOM/legal review, concrete PlatformServices adoption, governed-manifest/provenance emission, equivalent clean-build comparison, installer/signing/clean-machine evidence, clean-candidate provenance, and release signoff remain open.
- Product-completion Wave F records a governed ARDY defer decision: eight conditional packets are closed without adding product/runtime dependencies, the external environment packet remains partial, and signed legal review plus representative research, remediation retest, and bounded beta execution remain external. Wave G defines all ten final qualification authorities and truthfully retains an unfrozen candidate and unsigned undecided release decision until those external and clean-environment gates exist.
- PFU-02 now has one bounded asset-attachment revision slice: checked plans, stale-source rejection, safe operation IDs, idempotent project-local receipts, an Assets-panel review/confirm route, staged in-process payload/manifest rollback, and conservative project-local interruption recovery are implemented. It does not yet provide background/startup recovery or cross-file crash atomicity; keep those limitations explicit before widening attachment claims.
  - PFU-03 now has bounded native atlas-metadata, crop/nearest-neighbor scale, creator-authored ordered two-to-256 color fixed-palette and exact-RGBA frequency-extracted palette reduction with optional fixed-point Floyd-Steinberg dithering on either palette route, individual-tile slicing, PCM16 WAV trim/fade/gain, user-curation, and read-only comparison slices: the revision service hashes validated runtime-ready promoted payloads, stages deterministic PNG/WAV/metadata output, and publishes/reuses/removes un-attached immutable revisions plus their validated deterministic outputs without modifying source media; exact-grid tileset slicing produces row-major tile PNG directories that a reviewed project-assignment owner can normalize into fixed 48-by-48 runtime atlas cells, explicit and automatic palette mapping are deterministic but do not include perceptual clustering, while WAV metadata exposes bounded waveform, sample peak/RMS (not LUFS), and derived-loop sample-boundary metrics that remain a review signal rather than a listening test. User settings persist opaque favorites and named collections without establishing asset authority. Single-output PNG/WAV revisions now use a review/confirm project-attachment route, appear in contextual pickers, and retain a fail-closed attachment-reference ledger that blocks source-revision removal while a project reference is prepared or live; creator-facing recovery may finalize a prepared marker only from exactly one matching durable project manifest and otherwise leaves it protected. The Assets workspace can inspect a PCM16 source without writes, render its bounded waveform, drag-select exact trim frame fields before the explicit revision action, and display valid derived peak/RMS and loop-boundary QA. Codec conversion/loudness/voice/caption QA, cancellation/resume, safe detach tooling, wider custody workflows, and end-to-end creator/runtime/package evidence remain open.
  - PFU-03 transform staging cleanup is now explicit and fail-closed: an Assets action removes only direct revision-root children with a SHA-256 revision prefix and the service-owned `.png.tmp`, `.wav.tmp`, `.json.tmp`, or `.tiles.tmp` suffix. It preflights every matching candidate as a non-symlink file/directory before deletion and preserves final revisions, attachment ledgers, and unrelated temporary files. It neither resumes jobs nor recovers a partially published transform; cancellation/resume and broader recovery remain open.
  - PFU-03 also exposes the existing verified removal seam through a confirmation-gated selected-revision action. The core still reconstructs the safe asset/revision path and denies prepared or attached reference ledgers; this is not detach tooling, which remains open.
  - PFU-03 source inspection now includes a bounded read-only 32-by-24 PCM16 DFT-magnitude preview beside the waveform used for trim selection. It does not persist spectrogram data in a derived revision, establish a spectrogram editor, or provide codec/loudness/listening qualification.
  - PFU-03 now stores validated voice take metadata only on a runtime-ready, license-backed promoted audio manifest: locale, stable take ID, and an optional vetted muted-alternative promoted audio ID. The manifest license remains the rights authority, and metadata reaches a project only through the existing explicit attachment/replacement flow. The read-only Project Localization Audit exposes that attached metadata beside each saved Dialogue voice/caption pair and diagnoses absent/malformed metadata plus missing or invalid requested muted-alternative attachments; automatic project reconciliation, caption generation, locale fallback, playback/listening qualification, and broader rights workflows remain open.
- PFU-04 now has a bounded native F06 recipe command: the Ability workspace exposes selectable Starter Quest Choice and Town Event Signal typed gameplay-WYSIWYG templates with preview, idempotent apply, receipt-guarded revert, diagnostics, and first guided string/integer inputs. Parameter values can affect only named compatible rule fields and are range/key/binding checked before the concrete recipe changes. Its typed project document is saved atomically at `content/gameplay/recipes.json`, loaded only after validation, tracked by the normal dirty-state/recovery path, and cleared on close. The Mod workspace now separates native mod controls from a read-only MZ plugin scanner that reads only header metadata and exact static API tokens; it never executes source or evaluates `plugins.js`. F05 now has a bounded project-owned Menu Studio document at `content/ui/menus.json`: `menu_scene_graph` carries persisted canvas/pane rectangle/layer/focus data and active-scene metadata; runtime focus traversal, inspector editing/diagnostics, target-canvas presets, selected-pane canvas alignment, four reusable pane-layout templates, snapped-grid pane dragging and bounded lower-right resize, focus-flow preview, and a 64-entry local undo/redo history consume the same graph; diagnostics-file save is same-directory atomic; staged graph load fails before replacing live state; and the editor dirty/save/close/recovery lifecycle owns project menu changes. Quest Authoring now mutates its project-owned objective graph with add/remove node/link, condition, and reward actions plus a 64-entry local history; authoring flow diagnostics report missing completion, unreachable nodes, and no-completion-path risks without changing legacy runtime validation semantics. Dialogue Graph now provides panel-snapshot structural and topology diagnostics for invalid starts/targets, orphaned/dead-end nodes, nodes without an ending path, and missing keys when a valid native localization catalog is available, without changing existing preview traversal or runtime behavior. Its native Map-owned draft supports atomic `content/dialogues/<dialogue-id>.json` persistence, malformed-file rejection, private recovery, node/choice/start and choice-condition/effect mutations, preview routing, a 64-entry local history, and start/node localization-key selection from valid `content/localization` bundles through the normal dirty/save/close lifecycle. Multi-select, distribution and responsive constraints, components, persistent history, target-size evidence, parameterized template authoring/import, specialist domain integration, plugin trust/lock/load-order/support-file flow, additional stable reference pickers, localization/voice/caption linking, dialogue migrations/runtime execution, and quest migrations remain open; focused automated verification was rerun on 2026-07-16: recipes, Menu, Menu focus, Dialogue/Narrative, Quest graph/editor, and plugin compatibility passed; legacy compound filters matching zero tests are not passes. Manual target-size, controller, package, and release qualification remain open.

  - PFU-03 update: `tileset_slice` now has a dedicated reviewed project-assignment owner. It revalidates the promoted source, immutable grid manifest, deterministic tile filenames, and every tile SHA-256 before staging a project-owned `content/tilesets/<tileset-id>/tiles` bundle, a deterministically nearest-neighbor packed checked 48-by-48-cell `atlas.png`, provenance manifest, and receipt. Its reference ledger blocks derived-revision removal while prepared or assigned. This replaces the earlier “unassignable/multi-file derived assignment” limitation; crash recovery and detach remain open.
  - PFU-04 update: the read-only project localization audit now also lists persisted Dialogue node voice/caption pairs and diagnoses a missing governed audio attachment, a voice without a caption key, a caption without a voice, or a missing caption key. For a valid attached voice it exposes governed take locale/ID and diagnoses absent/malformed metadata plus missing or invalid requested muted-alternative attachments. It validates only project-contained, runtime-ready attached audio manifests and does not alter dialogue, localization bundles, assets, or playback. Rights qualification, locale fallback, manual playback qualification, and broader caption workflow remain open.
  - PFU-03 update: the Assets workspace can now show a read-only side-by-side source and latest source-scoped derived image (or first derived tileset tile) only after manifest-derived path validation, and its selected promoted-image preview supports a bounded drag crop selection that writes exact source-pixel fields only. Map insertion/history, runtime packing, and qualification remain open.
  - PFU-03 update: an assigned tileset bundle of any exact sliced cell size can now populate the active Perspective 2D Map’s persisted tileset page, palette, and default tile metadata through one undoable native import after project-manifest/grid/tile-path/48-cell packed-atlas validation. Assignment deterministically nearest-neighbor normalizes cells for the fixed Map renderer. The Map workspace exposes solid and per-direction persisted passage controls for a selected palette tile. The existing MapScene projects visible unlocked derived tiles into actual per-tile atlas render commands and its collision authority; direct player movement and Map route requests now check both the authored source exit and destination entrance, while legacy callers retain all-or-nothing passage behavior. The generic path graph exposes opt-in blocked cardinal transitions with deterministic diagnostics. Undo/redo, hide, and load clear stale projections. Cross-map propagation, detach recovery, and qualification remain open.
  - Dialogue choice labels can now retain optional stable localization keys through native picker, non-persistent interactive preview, project persistence/history, diagnostics, and the read-only localization audit; legacy freeform labels remain compatible. Broader runtime dialogue execution and manual localization qualification remain open. Current automated evidence (2026-07-16): `[localization]` passed 123 assertions in 24 cases and `[dialogue][narrative]` passed 12 assertions in 1 case.
  - A saved structurally valid native Dialogue Graph can now begin through the bound `MapScene` and existing message runtime, including visible keyboard-selectable choices and graph-target transitions. Integer-compatible choice conditions/effects use the existing `GlobalStateHub`, while unsupported conditions visibly disable a choice and malformed effects fail admission; a valid project `localization.default_locale` bundle automatically resolves stable node/choice keys, with an explicit creator preview override, preview fallback, and diagnostics. Node captions resolve through that catalog above the native message box and voice asset IDs dispatch through the injected native audio core, with missing/rejected playback observable but non-blocking. Its active project dialogue ID/conversation/node checkpoint now round-trips inside the existing MapScene save payload after global state, while stale/invalid checkpoints fail closed with a diagnostic rather than breaking an otherwise valid load. Visible, in-bounds Perspective 2D `start_dialogue`, `show_text`, supported state-only pages, and bounded current-map `transfer_player` pages now project to the native `confirm_interact` tile-input path, with final-match ordered selection for switch/integer-variable/event-local-self-switch conditions; a mixed page presents text before it starts the already-admitted saved graph, a state-only page consumes input without presentation, and transfer accepts only `current_map_id:x,y` in-bounds destinations. The runtime accepts only project-local `[A-Za-z0-9_-]` saved-dialogue IDs, rejects duplicate trigger tiles, reports missing/malformed/invalid graphs, and preflights before it applies preceding supported map switch/integer-variable/self-switch writes and transfers through the same state/movement authority. Versioned native dialogue writes and choice effects synchronize their switches, variables, and event-local self-switches into the Map preview without dirtying the authored document. Cross-map transfer, other event commands beyond bounded state writes/current-map transfer, transient presentation/choice-cursor save state, broader preview synchronization, and playthrough/package qualification remain open. Current automated evidence (2026-07-16): `dev-spatial` passed all 3 tests after stale expectations were repaired.
  - The Gameplay Recipe Gallery also offers a typed Camp Rest Recovery template. It uses the existing `companion_banter` WYSIWYG owner and bounds parameters to one named camp-status write and party-health resource delta; runtime camp/encounter execution remains explicitly out of this bounded scope. Current automated evidence (2026-07-16): `[gameplay][recipe]` passed 31 assertions in 3 cases.
  - Current PFU-04 Menu scope supersedes the residual wording above for bounded multi-select/distribution, responsive anchors, and parameterized native pane templates: those routes are implemented, while template import and the remaining broader capabilities stay open. Current automated evidence (2026-07-16) is recorded in the active PFU plan; manual/package qualification remains open.
- PFU-05 now has a native Map direct-placement slice: attached image drops and the selected-project Prop tool place a stable-ID Perspective 2D prop immediately in Props mode, atomically enrolling its project-path palette binding, projected transform, local undo/redo entry, dirty/recovery state, and serialized native draft data. Tiles retain their conservative empty-cell/idempotent conflict policy. Attached image drops in Events mode create a unique event on a visible unlocked event/object layer, persist its stable asset ID/project path in the same local history, and synchronize visible in-bounds image events into the bound `MapScene` as deterministic sprite commands. Each event retains a bounded horizontal sprite-sheet animation configuration (frame dimensions, one to 64 frames, duration, loop) that projects to native source rectangles and advances with the Map frame delta across unchanged projection batches. Each event may persist default and page-level visible/hidden sprite, plus block/pass-through collision overrides, which Event Authoring projects on visible in-bounds layers using the final matching switch/integer-variable/event-local-self-switch page for rendering, direct movement, and path planning. Editor-originated attached payloads carry the attachment manifest source SHA-256 and are rejected before Map mutation when the manifest is missing, malformed, or stale; only legacy programmatic callers without an open project context remain compatible without a revision. The read-only `ProjectAssetReferenceIndex` reports inbound/outbound asset edges and cautious orphan candidates across saved Perspective 2D maps, dialogue voice nodes, Character Creator appearance slots, Grid Part catalog definitions, and the audio-mix encounter-preview asset; its workspace queries do not authorize mutation. Prop loading rejects malformed arrays and invalid transform values before touching the existing Perspective 2D child document. Multi-instance brushes, cross-owner history, broader impact analysis, safe rename/delete plans, asset-catalog slicing, and package/release asset evidence remain open. Current automated evidence (2026-07-16): `dev-spatial` passed all 3 tests and `[asset_attachment]` passed 238 assertions in 11 cases.
  - Attached image events now also project from the Perspective 2D document into the bound native `MapScene` as a validated, deterministic event-sprite batch, following the existing owner-local undo/redo, draft load, and visible-layer state. Persisted bounded horizontal sprite-sheet animation values emit native source rectangles and advance from the Map frame delta across unchanged replacement batches. Default and page-level sprite visibility and `blocks_movement` values likewise project through the final matching page for native rendering, direct movement, and path planning; interaction/execution changes, asset-catalog slicing, package evidence, and release qualification remain open. Current automated evidence (2026-07-16): `dev-spatial` passed all 3 tests.
  - PFU-05 additionally exposes a reviewed selected-part Grid Part rectangle fill: bounds and every derived footprint are checked before the existing bulk command applies as one local undoable action, and the accessibility audit exposes labelled review/apply alternatives. Freehand brush strokes, controller/manual qualification, cross-owner history, and runtime/package behavior remain open. Current automated evidence (2026-07-16): `[grid_part]` passed 1035 assertions in 109 cases and `[accessibility]` passed 184 assertions in 33 cases.
  - The attached-asset manifest revision check is shared by the native Map, Character Creator appearance, Dialogue Graph voice, and Audio Mix assignment routes. These owners reject a missing, malformed, mismatched, or stale attachment revision before durable mutation; they do not form a cross-owner transaction or history stack. Current automated evidence (2026-07-16) is recorded in the active PFU plan; manual/package qualification remains open.
  - Assets now provides a typed read-only removal-impact preflight that enumerates indexed inbound owners and diagnostics, but it always denies deletion authority. A future safe remove/replace operation remains the responsibility of every affected domain owner.
  - The active Perspective 2D Map can replace its supported attached visual references through one local undoable owner operation after a fresh replacement attachment-revision check. The source remains attached, and all other Map/project owners, safe delete/rename, transaction breadth, runtime rendering, and verification remain open.
- PFU-06 now feeds native Perspective 2D Elevation Brush, Prop Placement, and Grid Part Placement snapshots—plus selected smart-prefab and current creator-plan review/apply alternatives—labelled Map mode/context state, and existing Menu Inspector visible focus rows into the creator Accessibility Audit with its existing audio/battle evidence. Aggregation normalizes focus ranges across owners without hiding same-owner duplicate-order findings. The unified Map mode selector supports focused Alt+1 through Alt+0 activation without intercepting text entry. This remains a bounded keyboard route, not controller-only operation, tile/event canvas alternatives, or assistive/manual review.
- PFU-07 has developer-only native creator tile, prop, and fixed message-event commands exposed from the Perspective 2D Map workspace: detached project-JSON application fails closed; reviewed deterministic `paint_tile`, separate `place_prop`, and separate `place message event` plans must resolve explicit active native palette/layer bindings and match the live document revision before the Map owner records one undoable edit. The prop route accepts only existing attached Perspective 2D prop-palette assets; the event route only creates one confirm-interact `show_text` event/page on a visible unlocked event/object layer; both derive stable IDs. Provider transport is dry-run only. Mixed domains, all other event logic, assistant tool-loop policy, RAG invalidation, provider qualification, runtime evidence, and verification remain open.
  - The reviewed deterministic tile route additionally accepts a bounded 1-to-64-cell same-tile rectangle. The planner rejects invalid/out-of-bounds dimensions before existing palette/revision/Map-owner admission; arbitrary shapes, freehand paint, provider-authored geometry, and verification remain open.
- PFU-05 now has a bounded native smart-prefab route through the Grid Part owner: versioned catalog definitions declare stable IDs, part dependencies, conflict tags, parameters, and operation groups; Level Builder reports preflight-ready and blocked operation IDs, then applies or rolls back the group together as one local undoable Map command with prefab provenance on every placed part. This is not project-wide prefab storage, runtime/package proof, safe delete/rename, or collaboration/branch history.
- PFU-04 now persists native Menu pane anchor/minimum-size constraints, has a shared target-canvas layout resolver, and renders a transient resolved target canvas with overflow/invalid-layout diagnostics without mutating authored geometry; Inspector editing remains in the existing history/runtime/project ownership. Responsive manual evidence, component systems, and broader Menu qualification remain open.
  - The four existing native pane templates now accept bounded margin and preferred-size overrides, producing ordinary persisted pane layouts through that same ownership route. Component libraries, template import/export, parameter expressions, and broader Menu qualification remain open.
- Dialogue Graph now has a bounded, session-local interactive preview for selected choices, integer conditions/effects, unsupported-condition diagnostics, and a 64-step cap. It does not establish final runtime dialogue execution, localization/audio presentation, playtest integration, or manual graph-playthrough evidence.
  - A disposable playtest session can write a redacted local support summary containing session state and diagnostic codes, while excluding local paths, raw process output, diagnostic messages/source paths, and object IDs. Full support-bundle, capture, redaction review, and external submission workflows remain open.
  - A native on-demand Localization Reference Audit compares valid project locale bundles with saved Dialogue/Quest stable key references, reporting missing keys, per-locale coverage gaps, missing locale font-profile declarations, malformed documents, and unused-key candidates without mutation. Other localization owners, import/export, full pseudo-localization workflows, glyph/RTL/IME/plural/layout checks, and manual review remain open.
  - The Localization Reference Audit now has a pure pseudo-localization visual-expansion preview. It does not persist a transformed bundle or establish RTL/IME/font/plural/layout readiness.
- The older `std::system`, compat audio lifetime, chatbot callback, QuickJS interruption, tracked generated-root, archive containment, and bounded LFS-reconciliation lanes are closed for their documented scope. Keep their guards passing; do not list them as current implementation blockers unless a regression is observed.
- Treat Phase 6 release-surface remediation as historical release evidence for the bounded `v0.1.0` lineage, not as proof that the current `development` branch is broadly clean. Keep `.\tools\ci\run_local_gates.ps1` passing before widening release claims.
- Treat `level_builder` as the native shippable grid-part map editor. Keep grid-part document editing, save/load/export, playtest, package readiness, diagnostics, and Perspective 2D handoff coherent.
- The release owner promoted `spatial_authoring` to a first-class Perspective 2D map editor surface on 2026-05-26. Both release IDs now deep-link into one creator-facing Map workspace; keep registry exposure, shared context, and explicit underlying document ownership aligned.
- Enforce the WYSIWYG done rule: a subsystem is not done without visual authoring, live preview, saved project data, runtime execution, diagnostics, and tests.
- Keep the AI assistant workflow visibly review-gated: approve/reject/approve-all/apply/revert controls, result diffs, and reverse patches must stay exposed in editor snapshots when AI tooling changes.
- Treat live chatbot provider integration as future work unless a concrete provider service lands behind `IChatService`; creator-command transport profiles are not the same as shipped live chatbot providers.
- Keep template expansion tied to starter manifests, runtime profiles, certification loops, readiness rows, and specs before claiming a template lane is product-ready.
- Keep bootstrap/dev surfaces visibly marked as non-production.
- Keep any future compat and migration limitations diagnostic-rich and documented; current public compat manager registries are closed for the claimed bridge scope.
- Keep editor release navigation aligned with `docs/release/EDITOR_CONTROL_INVENTORY.md`.
- Keep release-required asset checks, install smoke, package smoke, and native version metadata aligned with `README.md`, `docs/APP_RELEASE_READINESS_MATRIX.md`, and `docs/release/RELEASE_PACKAGING.md`.
- Keep LFS wording precise: this checkout currently has 307,388 LFS-tracked paths reconciled as governed promoted-library payloads or governance evidence, and `tools/ci/check_promoted_asset_library.ps1` validates promoted bundle rows without removing GitHub payloads. Release-required app assets are verified by the release asset gate; broader shipped-game claims still need exact project selection and hydration evidence.
- Treat release authoring persistence as a guarded surface: ability draft IO must
  keep path-specific `last_io` diagnostics, failed loads must preserve the last
  valid draft, and Level Builder load/export safeguards must keep rejecting
  unsafe documents without replacing the bound document.
- Convert repeated review feedback into scripts or docs under `tools/` and `docs/agent/`.

## What Not To Do

- Do not promote `PARTIAL` lanes to ready/full based only on fixture coverage.
- Do not demote Perspective 2D / `spatial_authoring` back to nested tooling without an explicit release-owner reversal.
- Do not mark a system done when it only has deterministic contracts, schemas, or headless tests; it also needs the WYSIWYG completion evidence in `content/readiness/wysiwyg_done_rule.json`.
- Do not call the repository LFS-free, generated-artifact-free, shell-execution-free, production cloud-sync-ready, live-service-ready, storefront-ready, or proprietary RPG Maker MZ runtime-parity-ready unless fresh commands and owner evidence prove those exact claims.
- Do not introduce hidden fallback behavior for missing assets, scripts, saves, or runtime binaries.
- Do not add new large agent instructions to `AGENTS.md`; link from `docs/agent/INDEX.md` instead.
