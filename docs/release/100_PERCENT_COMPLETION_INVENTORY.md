# 100 Percent Completion Inventory

Status Date: 2026-05-03
Authority: active remaining-work inventory for the 100-percent completion push

This inventory tracks the lanes called out by `docs/superpowers/plans/2026-05-01-100-percent-completion-implementation-plan.md`. `content/readiness/readiness_status.json` and `docs/RELEASE_READINESS_MATRIX.md` remain the machine-checked readiness authorities.

| Lane ID | Current Status | Exact Blocker | Required Implementation Evidence | Required Verification Command | Release Claim Impact |
| --- | --- | --- | --- | --- | --- |
| `presentation_runtime` | `READY` | None for the claimed Phase 1 scope. | Non-OpenGL headless/reference capture breadth and shell-owned scene permutation goldens. | `ctest --preset dev-snapshot --output-on-failure`; `.\tools\ci\run_presentation_gate.ps1` | May be claimed ready for the evidenced presentation scope. |
| `visual_regression_harness` | `READY` | None for the claimed Phase 1 scope. | Backend metadata, stable hashes, command-stream parity, and governed goldens. | `ctest --preset dev-snapshot --output-on-failure`; `.\tools\ci\run_presentation_gate.ps1` | May be claimed ready for the evidenced visual regression scope. |
| `gameplay_ability_framework` | `READY` | None for the claimed Phase 2 scope. | Authored task-graph sequencing, editor author/preview/save/load/apply/revert workflow, schema examples, diagnostics, and signoff. | `ctest --preset dev-all -R "Ability|ability|BattleScene|MapScene" --output-on-failure` | May be claimed ready for authored ability runtime/editor scope. |
| `governance_foundation` | `READY` | None for the claimed Phase 3 scope. | ProjectAudit and release-readiness signoff enforcement with reviewer/date/verification/result/docs-alignment metadata. | `ctest --preset dev-project-audit --output-on-failure`; `.\tools\ci\check_release_readiness.ps1`; `.\tools\ci\truth_reconciler.ps1` | May be claimed ready for signoff enforcement. |
| `character_identity` | `READY` | None for the claimed Phase 4 engine/editor scope. Final-quality portrait art and cohesive character art direction remain content backlog. | Creator-supplied portrait/field/battle import rows, governed promotion paths, source metadata, dimensions, attribution diagnostics, part-library accept/reject/archive/assign snapshots, persisted assignments, runtime preview composition, and closure signoff. | Focused character/asset CTest lane; Python importer unit tests; `.\tools\ci\truth_reconciler.ps1` | May be claimed ready for character identity and appearance-part authoring; must not claim final art content completeness. |
| `achievement_registry` | `READY` | None for the claimed Phase 7 engine scope. Proprietary store SDK credentials remain project configuration. | Shared provider-profile status validation and AchievementPanel diagnostics for disabled, dry-run, command, reviewed production, missing-credential, and unsupported-provider boundaries. | `ctest --preset dev-all -R "Achievement|achievement|Mod|mod|Analytics|analytics" --output-on-failure` | May be claimed ready for engine-owned achievement provider profile validation; service-account publishing remains outside engine-ready claims. |
| `accessibility_auditor` | `READY` | None for the claimed Phase 5 scope. | Release top-level panel contrast coverage through renderer-derived text/rect evidence plus existing live adapters and governance. | `ctest --preset dev-all -R "accessibility|Accessibility" --output-on-failure`; `.\tools\ci\check_accessibility_governance.ps1` | May be claimed ready for the evidenced accessibility-auditor scope. |
| `audio_mix_presets` | `READY` | None for the claimed Phase 5 scope. | Backend/device status fixtures, muted fallback diagnostics, and AudioMixPanel matrix exposure. | `ctest --preset dev-all -R "audio|Audio" --output-on-failure`; `.\tools\ci\check_audio_governance.ps1` | May be claimed ready for the evidenced audio mix preset/backend-matrix scope. |
| `export_validator` | `READY` | None for the claimed Phase 6 export validator scope. Production signing certificates, notarization accounts, and platform-store publication remain project configuration. | Raw/source/external-store rejection diagnostics, promoted bundle enforcement, missing attribution and unresolved LFS pointer enforcement, signing/notarization profile enforcement, artifact policy profile enforcement, and package smoke evidence. | `ctest --preset dev-export --output-on-failure`; `.\tools\ci\check_release_required_assets.ps1`; `.\tools\ci\check_package_smoke.ps1 -BuildDirectory build/dev-ninja-release -PackageRoot build/package-smoke` | May be claimed ready for governed export validation and release artifact policy enforcement; do not claim repository-owned production credentials or store publication. |
| `mod_registry` | `READY` | None for the claimed Phase 7 engine scope. External marketplace services, payments, reviews, and proprietary platform publishing remain project configuration. | `ModMarketplaceProviderProfile` status validation and ModManagerPanel diagnostics for disabled, dry-run, command, reviewed production, missing-credential, and unsupported-provider boundaries. | `ctest --preset dev-all -R "Achievement|achievement|Mod|mod|Analytics|analytics" --output-on-failure` | May be claimed ready for engine-owned mod provider profile validation; live marketplace operation remains outside engine-ready claims. |
| `analytics_dispatcher` | `READY` | None for the claimed Phase 7 engine scope. Qualified public privacy/legal approval remains release-owner-waived. | Analytics endpoint provider status validation, AnalyticsPanel diagnostics, and explicit legal/privacy boundary docs. | `ctest --preset dev-all -R "Achievement|achievement|Mod|mod|Analytics|analytics" --output-on-failure`; `.\tools\ci\truth_reconciler.ps1` | May be claimed ready for engine-owned analytics profile validation; legal approval remains separate from engine readiness. |
| `template_expansion_and_upgrade` | `READY` | None for the claimed Phase 8 template scope. Phase 10 separately owns app-route wiring for WYSIWYG showcase surfaces. | RPG and non-RPG starter templates, generated starter manifests, runtime profiles, certification loops, WYSIWYG showcase bindings, owner decisions for previous partial template rows, and aligned docs/readiness records. | Template certification/spec/readiness gates plus `.\tools\ci\truth_reconciler.ps1`; final verification must be rerun before any release tag. | May be claimed ready for the current starter-template scope; Phase 10 WYSIWYG app-route closure remains mandatory below, while final release remains blocked by Phases 10, 11, and 12. |
| `release_asset_and_local_intake_governance` | `READY` | None for the claimed Phase 9 asset-governance scope. Broad raw drops remain local quarantine until curated attribution and bundle manifests exist. | `SRC-007` RAR recovery, `SRC-011` and `SRC-012` local source manifests/catalogs, `BND-005` governed CC0/public-domain promoted subset, capped generated catalog shards, mandatory-scope lock, release-required asset report, and source registry/docs updates. | `ctest --preset dev-all -R "(AssetLibrary|export|Export)"`; `.\tools\ci\check_asset_library_governance.ps1`; `.\tools\ci\check_release_required_assets.ps1`; `git diff --check`. | May be claimed ready for local-use cataloging and the promoted bounded starter subset; must not claim all raw assets are release/export ready. |
| `phase_10_wysiwyg_roadmap_completion` | `MANDATORY_OPEN` | Template showcase surfaces are not wired to registry-backed editor panel routes: the showcase still records class-style `editor_panel` strings without `editor_panel_registry_id` values, and many candidate registry entries remain hidden behind `Deferred` exposure instead of routable `ReleaseTopLevel` or `Nested` surfaces. | Every claimed WYSIWYG showcase surface must name a valid registry-backed editor panel route, expose that route as `ReleaseTopLevel` or `Nested`, preserve the WYSIWYG done-rule evidence across all `READY` subsystem records, and keep focused regression coverage in `tests/unit/test_s32_wysiwyg_lanes.cpp`. | `ctest --preset dev-all -R "WYSIWYG|readiness_status|template" --output-on-failure`; `.\tools\ci\truth_reconciler.ps1`; `.\tools\ci\run_local_gates.ps1` before release tagging. | Must not be claimed ready until every showcase surface is app-routable through the editor panel registry; no final 100-percent release claim until Phases 10, 11, and 12 are complete. |

Phase 4 focused character/asset CTest command:

```powershell
ctest --preset dev-all -R "Character|character|AssetLibrary" --output-on-failure
```

## Mandatory Open Lanes

These lanes are mandatory for the full 100-percent program scope. They may be outside the currently packaged release payload until their evidence exists, but they are not optional and must not be described as optional follow-on work. Phase 8 and Phase 9 are complete for their claimed scopes; the remaining work is Phase 10 through Phase 12.

| Lane ID | Current Status | Exact Blocker | Required Implementation Evidence | Required Verification Command | Release Claim Impact |
| --- | --- | --- | --- | --- | --- |
| `phase_11_offline_tooling_pipelines` | `MANDATORY_OPEN` | Retrieval, segmentation, and audio tooling must remain offline, artifact-producing, and job-runner-addressable. | Shared offline job runner coverage for retrieval, SAM/SAM2-compatible segmentation manifests, and Demucs/Encodec-compatible audio manifests; runtime consumes exported artifacts only. | Python tool CTest lane; `ctest --preset dev-all -R "offline|retrieval|vision|audio|job_runner|tools" --output-on-failure`; `.\tools\ci\check_mandatory_completion_scope.ps1`. | No final 100-percent tooling claim until approved offline lanes are verified through shared jobs and artifact contracts. |
| `phase_12_curated_content_release_gate_and_tag` | `MANDATORY_OPEN` | Raw/cataloged assets remain local-use-only until curated subsets have attribution evidence and bundle manifests; current `main` must be rerun through final gates and tagged by the release owner. | Per-bundle attribution records, checksums, package destinations, release eligibility metadata, promoted payloads under `imports/normalized/`, fresh final gate outputs on the exact commit to tag, updated release-readiness report, and annotated release or prerelease tag. | `.\tools\ci\check_asset_library_governance.ps1`; `.\tools\ci\check_release_required_assets.ps1`; focused asset/export CTest lanes; `pre-commit run --all-files`; `.\tools\ci\run_local_gates.ps1`; `.\tools\ci\run_presentation_gate.ps1`; `.\tools\ci\run_release_candidate_gate.ps1`; `git tag -a`. | No final content-complete or tagged-release claim until every desired bundled content category has governed promoted bundles and final gates pass on the tagged commit. |
