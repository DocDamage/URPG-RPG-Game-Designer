# Archived Probes

This folder holds one-off tooling or investigation probes that are no longer active entry points.

Keep new maintained tooling under the normal feature folders in `tools/`. Files here are preserved only for traceability.
