# Tools Archive

This folder stores local or superseded tooling-side scratch artifacts that are kept only for traceability.

Current live tooling should continue to live under the normal `tools/` feature folders. Archived probes here should not be treated as maintained entry points.
