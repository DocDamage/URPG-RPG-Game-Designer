# Retrieval Shared Helpers

Shared retrieval-side helpers live here.

Current scope:

- deterministic tokenization
- lightweight embedding generation
- query scoring and bundle loading

These helpers keep the retrieval lane usable even before a full FAISS dependency is installed.
