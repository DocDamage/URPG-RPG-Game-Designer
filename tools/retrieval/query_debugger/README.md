# Retrieval Query Debugger

This directory contains local query/debug tooling for retrieval bundles.

Current scope:

- load a generated retrieval bundle
- embed a query with the adapter declared by the bundle
- print top matching chunks with source paths and scores
