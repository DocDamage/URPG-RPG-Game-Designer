# asset_sources

Governance directory for URPG asset and external repository intake.
See docs/PROGRAM_COMPLETION_STATUS.md (P3-02, P3-03) for program context.

Current local source IDs include `SRC-013` for the `itch/loose` CC0 PNG drop promoted through `BND-008`, `SRC-014` for present repo raw payloads promoted through `BND-010`, and `SRC-015` for sibling-folder bulk assets promoted through `BND-011`.

This directory is intentionally created empty; content will be added through the governed intake pipeline.
