# compat fixtures

Governed landing zone for externally sourced compat fixtures.

Only assets or code samples with a recorded `fixture_only` disposition may be staged here, and each fixture must carry plugin fixture metadata before it is used in tests.
