#include "audio_inspector_model.h"
#include <mutex>

namespace urpg::editor {

// Implementation split for model logic (e.g. projecting asset paths from asset store sync)
// For now, simpler than Message/Save since Audio is a runtime-heavy probe rather than static schema projection.

} // namespace urpg::editor
